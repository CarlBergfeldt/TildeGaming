# Project working instructions

## Validation requires confirmation

After completing game implementation work, stop and ask the user whether they want validation run. Do not automatically run builds, automated tests, browser playtests, screenshot reviews, or other game-validation passes unless the user confirms.

Implementation may include lightweight syntax or compile checks only when they are necessary to continue making the requested changes. Treat final validation as a separate, user-approved step.
